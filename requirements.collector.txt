# ============================================================
# requirements.collector.txt
# Mirpur-10 Traffic Collector — Cloud Run Job
# Python 3.11 | pinned for reproducibility
# ============================================================

# Supabase client
supabase==2.3.6

# Environment variable loading (used by config.py)
python-dotenv==1.0.1

# HTTP / REST calls (Mapbox, WeatherAPI)
requests==2.31.0

# Numerical / tabular processing
numpy==1.26.4
pandas==2.2.2

# Scheduler (not used in Cloud Run one-shot mode, but imported by pipeline.py)
schedule==1.2.1
